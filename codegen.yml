schema: "src/graphql/**/*.graphql"
generates:
  src/graphql/generated.ts:
    plugins:
      - "typescript"
      - "typescript-resolvers"
    config:
      avoidOptionals: true
      enumsAsTypes: true
      immutableTypes: true
      typesPrefix: "GQL_" # adds a prefix to all types