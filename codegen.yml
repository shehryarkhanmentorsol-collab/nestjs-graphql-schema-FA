overwrite: true
schema: "./src/**/*.graphql"
generates:
  src/graphql/generated.ts:
    plugins:
      - "typescript"
      - "typescript-resolvers"
    config:
      typesPrefix: GQL_
      useIndexSignature: true
      contextType: any